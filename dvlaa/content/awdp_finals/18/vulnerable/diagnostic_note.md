# Diagnostic kit

Operators keep one versioned runbook for each diagnostic recipe. A routine
workflow creates a draft, records its directory target, obtains a release
decision through the operations process, and then stores the returned probe
report with the version.

The service directory identifies the diagnostic destinations available to an
operator.
Probe reports are evidence records: an outcome does not create a future
publication decision.
