# Upstream notice

The files `docker-compose.yaml`, `docker-compose-template.yaml`,
`generate_docker_compose`, `certbot/`, `nginx/`, `ssrf_proxy/`,
`startupscripts/`, and `volumes/sandbox/conf/config.yaml.example` are copied
from the official Dify repository at tag `1.9.2`
(commit `ec871819e46de82f2f9ee33a71a61d96a58bdb37`):

<https://github.com/langgenius/dify/tree/1.9.2/docker>

They remain under the upstream project's license. `VERSION` records checksums
for the generated Compose files. DVLAA-specific files in this directory are
the local `.env.example`, `.gitignore`, `README.md`, `bootstrap.sh`,
`healthcheck.sh`, `init-admin.sh`, and `provision.py`.
